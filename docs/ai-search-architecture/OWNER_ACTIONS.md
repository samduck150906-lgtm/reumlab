# 운영자만 처리할 수 있는 항목

계정 권한·운영 배포·실서버 확인이 필요해 이 작업에서 **하지 않았거나 할 수 없었던** 것들이다.
"구조를 만들었다" 와 "검색엔진·AI 가 실제로 추천한다" 는 다르므로, 아래는 완료로 표기하지 않았다.

## A. 배포 (권한 경계)

| # | 할 일 | 비고 |
|---|---|---|
| A1 | 이 브랜치를 검토하고 원격 push / PR / main merge 결정 | 브랜치 `claude/ai-search-architecture`. 지시 없이 push·merge 하지 않았다 |
| A2 | main 배포 시 **IndexNow 자동 제출이 함께 나간다**는 점 확인 | `netlify.toml` `[context.production]` = `npm run build && npm run seo:indexnow` (네이버·Bing) |
| A3 | 배포 후 `DEPLOY_AND_ROLLBACK.md` 3절 12개 항목 실서버 확인 | 로컬 `out/` 검증과 실서버 응답은 다른 검사다 |

## B. 실제 접수 확인 (QA F05 = NOT_RUN)

| # | 할 일 | 비고 |
|---|---|---|
| B1 | 배포 후 상담 폼 **실제 1건 제출** → Netlify Forms 접수 내역에 새 필드 5종(`홈페이지주소상태`, `홈페이지환경`, `수정권한`, `관심패키지`, `지속관리관심`)이 저장되는지 확인 | 로컬 검사는 목 서버 200 응답까지만 확인했다. **이것을 접수 성공으로 간주하지 않았다** |
| B2 | 새 필드가 영업 알림 메일 템플릿에 포함되는지 확인 | 필요하면 Netlify 알림 설정 수정 |
| B3 | 테스트 제출 건은 접수 내역에서 삭제 | — |

## C. 검색엔진 등록 (권한 필요)

| # | 할 일 | 비고 |
|---|---|---|
| C1 | Google Search Console → URL 검사 → `https://reumlab.com/ai-search-optimization/` 색인 요청 | 사이트맵 등록은 코드에서 끝났지만 색인 요청은 계정 작업 |
| C2 | 네이버 서치어드바이저 → 웹페이지 수집 요청 | 소유확인 태그·RSS·사이트맵은 기존 그대로 보존됨 |
| C3 | 색인 상태·노출·클릭은 실제 계정에서 확인될 때까지 **미확인**으로 둔다 | 이 작업에서는 확인할 수 없었다 |

## D. 이전 작업에서 남아 있는 항목

| # | 할 일 | 비고 |
|---|---|---|
| D1 | 커밋 `16d04a4`(가이드 FAQ·LocalBusiness 전환·홈 FAQ no-JS 대비)가 **로컬에만 있고 원격에 없다** | 이 브랜치의 기준선이다. 함께 올릴지 결정 필요 |
| D2 | `/guide/` 신규 FAQ 6문항 배포 후 GSC 색인 요청 | — |

## E. 이번 범위 밖이지만 기록해 두는 개선점

| # | 내용 | 근거 |
|---|---|---|
| E1 | **폰트 서브셋** — `/fonts/PretendardVariable-1.3.9.woff2` 가 **2.0MB** 이고 사이트 전 페이지의 LCP 를 지배한다. 한글 상용 + Latin 서브셋 또는 `unicode-range` 분할을 하면 Performance 가 전 페이지에서 오른다 | 모바일 Lighthouse 중앙값: 신규 76 / `/geo-website/` 81 / `/ai-voice-development/` 78. 셋 다 LCP ≈5s 이고 병목이 같다 |
| E2 | **공용 상담 폼 제출 버튼 대비** — `bg-accent`(`#3d7cff`) 위 흰 글자 = **3.80:1** 로 WCAG AA(4.5:1) 미달. 같은 팔레트의 `accent.deep`(`#2563eb`)는 **5.17:1** 로 통과한다 → `components/LandingInquiryForm.tsx` 의 제출 버튼 `bg-accent hover:bg-accent-deep` 을 `bg-accent-deep hover:bg-accent` 순으로 바꾸면 해소. **모든 랜딩의 버튼 색이 함께 바뀌므로** 디자인 확인 후 진행 | Lighthouse axe `color-contrast` 실패 1건. 대비값은 WCAG 상대휘도 공식으로 직접 계산 |
| E3 | **홈 JS 오류** — `script.js:679` 의 `function updateMobileCta()` 가 `if (mcta) {}` 블록 안에 선언돼, 미니파이 시 `"use strict"` 가 붙으면서 `script.js:693` 에서 `ReferenceError`. `var updateMobileCta = function () { … }` 로 바꾸거나 선언을 블록 밖으로 올리면 해결. **이번 브랜치에서 `script.js` 는 한 줄도 바꾸지 않았고 기준선에서도 동일하게 재현된다** | 영향: 모바일 고정 CTA 의 "문의 영역 보이면 숨김" 만 동작 안 함 |
| E4 | **`npm run build` 에서 `prebuild` 가 두 번 실행된다** — `build` 스크립트가 `npm run prebuild` 를 직접 부르는데 npm 라이프사이클도 자동으로 부른다. `build` 에서 명시 호출을 빼면 빌드 시간이 줄어든다 | 선행 사항, 이번 작업과 무관 |

## F. 확인이 필요한 정책 항목

| # | 내용 |
|---|---|
| F1 | `/privacy/` 의 수집 항목에 이번에 늘어난 필드(홈페이지 주소·환경·수정 권한·관심 패키지·지속 관리 관심)를 반영할지 검토. 전부 **사업 정보**이고 개인 식별 정보를 새로 늘리지 않았지만, 실제 정책 문구와 대조는 운영자 판단이 필요하다. 보유 기간·해외 이전·수탁자 조건은 **확인할 수 없어 임의로 만들지 않았다** |
| F2 | CARE / CARE PLUS 의 기간·해지·환불·실비 조건은 화면에 "계약으로 정한다" 로만 적었다. 실제 계약서 문구와 맞추는 것은 운영자 작업 |
| F3 | 상담 폼에서 **비밀번호·API 키·인증코드는 받지 않는다**(게이트가 강제). 계약 후 접근 권한은 안전한 초대 방식으로 받는다는 안내가 화면에 있다. 실제 온보딩 절차 문서화 필요 |
