# 메타(페이스북) 픽셀 · 웹사이트 전환 광고 · UTM 가이드

`reumlab.com` 사이트의 광고 추적 세팅 정리. **코드 쪽은 이미 다 적용**되어 있고,
메타 광고 관리자에서 할 일만 따라 하면 됩니다.

---

## 1. 메타 픽셀 (이미 설치됨)

- 픽셀 ID: **`1021548000778850`**
- 사이트 **모든 페이지**에 픽셀 기본 코드가 자동 삽입됩니다. (`components/Analytics.tsx`)
  배포되면 모든 페이지에서 `PageView`가 자동으로 집계됩니다.
- 다른 픽셀로 바꾸려면 Netlify 환경변수 `NEXT_PUBLIC_META_PIXEL_ID`에 새 ID를 넣으면
  코드 기본값을 덮어씁니다. (그대로 쓸 거면 아무것도 안 해도 됩니다.)
- ⚠️ 픽셀을 **GTM에도 중복으로 넣지 마세요.** 이중 집계됩니다. (여기선 직접 설치 방식)

### 설치 확인
1. 메타 광고 관리자 → **이벤트 관리자** → 픽셀 선택
2. 배포 후 사이트를 한 번 방문하면 최대 20분 내 상태가 **활성**으로 바뀝니다.
3. 크롬 확장 **Meta Pixel Helper**로 페이지에서 픽셀·이벤트 발동을 바로 확인할 수 있습니다.

---

## 2. 전환 이벤트 = `Lead` (이미 적용됨)

웹사이트 전환 광고는 "무엇을 전환으로 볼지"가 핵심입니다. 우리 전환은 **무료 진단 신청 폼 제출**입니다.

- `/soho` 폼이 **정상 제출되면** 자동으로 `fbq('track', 'Lead')`가 발동합니다.
  (메타 표준 이벤트 중 **잠재 고객(Lead)** — "나중에 연락받을 수 있음을 알고 정보를 제출" = 우리 폼과 정확히 일치)
- 즉 광고 → 사이트 방문(`PageView`) → 폼 제출(`Lead`) 흐름이 그대로 측정됩니다.

### 광고 만들 때
1. 캠페인 목표: **잠재 고객(Leads)** 또는 **판매/전환**
2. 전환 위치: **웹사이트**
3. 전환 이벤트: **`Lead`** 선택
4. (선택) 이벤트 관리자에서 `Lead` 기반 **맞춤 전환**을 만들어 `/soho` URL 조건을 걸면
   소호 폼만 따로 집계할 수 있습니다.

> 광고를 막 시작하면 `Lead` 이벤트가 "활동 없음"일 수 있습니다. 실제 제출이 1건 들어와야
> 이벤트 관리자에 잡힙니다. 테스트로 직접 한 번 신청해 확인하세요.

---

## 3. UTM 링크 (광고 클릭 출처 추적)

UTM은 **광고 링크 뒤에 붙이는 꼬리표**입니다. 어떤 캠페인·소재에서 들어왔는지 구분해 줍니다.
**코드가 아니라 메타 광고 관리자에서** 설정합니다.

### 우리 사이트 추천 도착 URL
무료 진단이 목표라면 광고는 `/soho`로 보내는 게 좋습니다:

```
https://reumlab.com/soho/?utm_source=meta&utm_medium=cpc&utm_campaign=soho_free_diagnosis&utm_content=video_a
```

| 파라미터 | 뜻 | 예시 |
|---|---|---|
| `utm_source` | 유입 매체 | `meta`, `instagram`, `facebook` |
| `utm_medium` | 유형 | `cpc`(유료클릭), `paid_social` |
| `utm_campaign` | 캠페인 이름 | `soho_free_diagnosis` |
| `utm_content` | 소재 구분(A/B) | `video_a`, `image_b` |
| `utm_term` | (선택) 키워드 | `소상공인_홈페이지` |

### 메타 광고 관리자에서 넣는 위치
- **광고 단위** 편집 화면 → **웹사이트 URL**에 `https://reumlab.com/soho/` 입력
- 그 아래 **URL 매개변수** 칸에 아래만 입력 (물음표 `?` 없이):
  ```
  utm_source=meta&utm_medium=paid_social&utm_campaign=soho_free_diagnosis&utm_content={{ad.name}}
  ```
  > `{{ad.name}}` 같은 **동적 파라미터**를 쓰면 소재 이름이 자동으로 들어가 편합니다.

### 신청서에 출처가 같이 남습니다 (이미 적용됨)
폼이 제출될 때 URL의 `utm_*`·`fbclid` 값이 **함께 Netlify로 전송**됩니다.
→ Netlify **Forms → soho-diagnosis** 의 각 신청 건에 `utm_source`, `utm_campaign` 등이 표시되어,
**어떤 광고에서 온 신청인지** 한눈에 볼 수 있습니다. (세션 동안 유지되어 다른 페이지를 거쳐도 보존)

---

## 4. 요약 — 지금 할 일

- [x] 픽셀 설치 (코드 적용 완료 — 배포만 하면 활성화)
- [x] `Lead` 전환 이벤트 (폼 제출 시 자동 발동)
- [x] UTM 신청서 기록 (자동 첨부)
- [ ] **배포** 후 이벤트 관리자에서 픽셀 **활성** 확인
- [ ] 광고 만들 때 전환 이벤트 **`Lead`** 선택, 도착 URL `https://reumlab.com/soho/`
- [ ] 광고 **URL 매개변수**에 UTM 입력
- [ ] (선택) 테스트 신청 1건으로 `Lead` 발동 + Netlify에 UTM 기록 확인
