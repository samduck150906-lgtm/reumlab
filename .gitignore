.DS_Store
Thumbs.db
*.log
.env
.env.*
!.env.example
node_modules
.next
out
tsconfig.tsbuildinfo
# 빌드 시 재생성 (랜딩 수만큼 커짐)
public/feed.xml

# Local Netlify folder
.netlify

# Generated output files (marketing campaigns, reports)
.output/

# Search Console / Analytics exports contain private business telemetry.
# The report generator reads this local folder but never publishes raw rows.
docs/seo-aeo/private/

# NEO HTTP snapshots and Search Advisor exports are private operational data.
docs/neo/private/

# GEO/AI 실측 원문·캡처·실행 데이터는 정적 배포와 Git에 포함하지 않는다.
.geo-measurements/
